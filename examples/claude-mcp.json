{
  "mcpServers": {
    "os-exec": {
      "type": "stdio",
      "command": "node",
      "args": ["${OS_EXEC_MCP_HOME}/dist/mcp/stdio.js"],
      "env": {
        "OS_EXEC_WORKSPACE_ROOT": "${CLAUDE_PROJECT_DIR:-.}"
      }
    }
  }
}
