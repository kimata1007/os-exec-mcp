{
  "mcpServers": {
    "os-batch": {
      "type": "stdio",
      "command": "node",
      "args": ["${OS_BATCH_MCP_HOME}/dist/mcp/stdio.js"],
      "env": {
        "OS_BATCH_POLICY_FILE": "${OS_BATCH_MCP_HOME}/examples/policy.development.json",
        "OS_BATCH_WORKSPACE_ROOT": "${CLAUDE_PROJECT_DIR:-.}"
      }
    }
  }
}
